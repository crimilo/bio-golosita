{
	"name": "bio-golosita",
	"type": "module",
	"version": "1.0.0",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && astro preview",
		"astro": "astro",
		"assets": "node scripts/fetch-fonts.mjs && node scripts/process-images.mjs && node scripts/posters.mjs && node scripts/favicons.mjs",
		"og": "node scripts/og/generate.mjs",
		"deploy": "astro build && wrangler deploy"
	},
	"dependencies": {
		"astro": "^7.3.0"
	},
	"devDependencies": {
		"wrangler": "^4.127.1"
	},
	"allowScripts": {
		"esbuild": true
	}
}
