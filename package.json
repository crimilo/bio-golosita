{
	"name": "bio-golosita",
	"type": "module",
	"version": "1.0.0",
	"engines": {
		"node": ">=22.12.0"
	},
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro build && astro preview",
		"astro": "astro",
		"assets": "node scripts/fetch-fonts.mjs && node scripts/process-images.mjs && node scripts/posters.mjs && node scripts/favicons.mjs",
		"deploy": "astro build && wrangler pages deploy dist --project-name bio-golosita"
	},
	"dependencies": {
		"astro": "^7.2.9"
	},
	"devDependencies": {
		"wrangler": "^4.127.1"
	},
	"allowScripts": {
		"esbuild": true
	}
}
